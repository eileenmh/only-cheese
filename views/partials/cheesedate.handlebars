
<div class="gallery">
  {{#each users}}
    <div class="profile">
      <img src="{{profilePic}}" alt="{{name}}">
      <h2>{{name}}</h2>
      <p>{{bio}}</p>
      <a href="{{profileLink}}">View Profile</a>
    </div>
  {{/each}}
</div>
